# Image-owned override of Fedora's 8 GiB default. zram-generator evaluates
# `ram` from MemTotal in MiB; this retains the hardware bound while letting
# machines with at least 16 GiB RAM expose a 16 GiB compressed-swap device.
[zram0]
zram-size = min(ram, 16384)
